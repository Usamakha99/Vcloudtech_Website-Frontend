/** Phones only: skip intro. Desktop: hide page until intro overlay plays. */
export const INTRO_BLOCK_SCRIPT = `(function(){try{if(window.matchMedia("(max-width:767px)").matches){document.documentElement.classList.add("intro-skip-mobile");document.documentElement.classList.remove("intro-pending");return}document.documentElement.classList.add("intro-pending")}catch(e){document.documentElement.classList.remove("intro-pending")}})();`;
