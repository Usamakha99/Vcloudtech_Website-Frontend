/** Phones only: skip intro. Desktop home (/): hide page until intro overlay plays. */
export const INTRO_BLOCK_SCRIPT = `(function(){try{var path=window.location.pathname;if(path!=="/"){document.documentElement.classList.remove("intro-pending");return}if(window.matchMedia("(max-width:767px)").matches){document.documentElement.classList.add("intro-skip-mobile");document.documentElement.classList.remove("intro-pending");return}document.documentElement.classList.add("intro-pending")}catch(e){document.documentElement.classList.remove("intro-pending")}})();`;
